# Archived ext-fibo-release-local draft

This directory preserves the exact v0.2.0 module bytes formerly present in the
active finance discovery tree. The artifact is rejected historical evidence:
it is not an active M2 module, is not part of the v0.3.0 ten-module registry,
and must not be used as approval or release evidence.

FIBO alignment evidence is governed by
`docs/ontology/references/references.lock.yaml` and reviewed alignment records.
Any future formal-import adapter requires a separate review and must not
wholesale-import FIBO.
