# SHACL / OWL Engine Pin (M2-PLAN §11 Trial→Adopt)
#
# Status: Trial (pinned selection) — execution evidence pending Python/Java runtime on this host
# Product: Stop-Ship until §0.1; this file does NOT claim Adopt or CI green SHACL enforcement.
#
# Decision date: 2026-07-30 (Round-3)
# Authority: M2-PLAN row "真实 SHACL-SPARQL runner"

primaryReferenceValidator:
  id: pyshacl
  product: "RDFLib/pySHACL"
  decision: Trial
  pinReason: >-
    W3C SHACL Rec alignment via RDFLib; supports sh:sparql and custom ConstraintComponent
    needed for ADR-012 Tier-2 (NoFutureKnowledge / AvailabilityBeforeUse). Node rdf-validate-shacl
    remains fast parse/Tier-1 only — insufficient for SPARQL-capable shapes.
  targetVersions:
    python: ">=3.10,<3.13"
    pyshacl: "0.26.0"   # pin candidate — lock exact wheel hash in CI when runtime available
    rdflib: ">=7.0.0,<8"
  installHint: "pip install 'pyshacl==0.26.0'"
  ciEntry: "scripts/domain/run-pyshacl-smoke.cjs"
  evidenceStatus: executed-via-smoke
  evidenceNote: >-
    Round-4: Python 3.12 + pyshacl==0.26.0 installed locally; scripts/domain/run-pyshacl-smoke.cjs runs
    docs/domain/infrastructure/smoke/{shapes,data-good,data-bad}.ttl. Domain-corpus Adopt still pending.

crossCheckValidator:
  id: apache-jena-shacl
  product: "Apache Jena SHACL"
  decision: Trial-cross-check
  pinReason: >-
    Independent Java implementation for release-candidate / import-closure changes (M2-PLAN OWL/SHACL dual check).
  targetVersions:
    jena: "5.2.0"       # pin candidate
  evidenceStatus: pending-java-runtime

owlConsistency:
  id: robot-owlapi
  decision: Assess
  note: "Not pinned this round; do not claim DL consistency."

nonClaims:
  - "Do not treat structural negatives marked 'SHACL engine execution pending' as machine-enforced."
  - "Do not cite this pin file as Adopt until CI runs pinned pySHACL against domain shapes + fixtures."
  - "Slice A JSON staging is not a substitute for SHACL validation of RDF graphs."
