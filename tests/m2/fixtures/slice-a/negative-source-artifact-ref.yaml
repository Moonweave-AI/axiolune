caseId: negative-source-artifact-ref
extends: positive-market-instrument-contract.yaml
expected:
  valid: false
  codes: [FACT_SOURCE_ARTIFACT_REF]
mutations:
  - op: set
    path: micEntries[0].source.artifactRef
    value: urn:forbidden-plain-string
