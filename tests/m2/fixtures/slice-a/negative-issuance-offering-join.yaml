caseId: negative-issuance-offering-join
extends: positive-market-instrument-contract.yaml
mutations:
  - op: set
    path: issuances[0].originatingOffering
    value: urn:offering:missing:v1
expected:
  valid: false
  codes: [ISSUANCE_OFFERING_JOIN]
